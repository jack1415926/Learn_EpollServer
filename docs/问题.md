为什么使用epoll LT，而不是ET。
  - 为什么网络线程和业务线程池分离。
  - 连接序列号如何防止fd复用后的旧响应串发。
  - Redis为什么必须在fork后初始化。
  - 为什么同步Redis Lua让QPS下降约46%，后续可如何优化。
  - 为什么6个客户端进程优于8个。
  - 为什么该QPS是VM本地阶段性基准，不是服务器理论上限。
  - 为什么 Redis 异常选择 fail-open，而不是 fail-close？

    为什么 L1 在读取报文前关闭？

    L1 黑名单怎么产生、TTL 怎么设计？

    Redis Lua 为什么能保证原子性？

    为什么不能简单 GET + INCR？